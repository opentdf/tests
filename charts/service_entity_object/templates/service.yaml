apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_entity_object.fullname" . }}
  labels:
    {{- include "service_entity_object.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_entity_object.selectorLabels" . | nindent 4 }}
