apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service_entity_object.fullname" . }}-test-connection"
  labels:
    {{- include "service_entity_object.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service_entity_object.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
