apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service_remote_payload.fullname" . }}-test-connection"
  labels:
    {{- include "service_remote_payload.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service_remote_payload.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
