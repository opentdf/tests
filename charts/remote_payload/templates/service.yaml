apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_remote_payload.fullname" . }}
  labels:
    {{- include "service_remote_payload.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_remote_payload.selectorLabels" . | nindent 4 }}
