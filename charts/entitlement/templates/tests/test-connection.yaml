apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service_entitlement.fullname" . }}-test-connection"
  labels:
    {{- include "service_entitlement.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service_entitlement.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
