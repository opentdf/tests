apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_entitlement.fullname" . }}
  labels:
    {{- include "service_entitlement.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_entitlement.selectorLabels" . | nindent 4 }}
