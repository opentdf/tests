apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_entity.fullname" . }}
  labels:
    {{- include "service_entity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_entity.selectorLabels" . | nindent 4 }}
