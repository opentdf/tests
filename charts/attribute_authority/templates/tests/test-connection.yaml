apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "service_attribute_authority.fullname" . }}-test-connection"
  labels:
    {{- include "service_attribute_authority.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "service_attribute_authority.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
