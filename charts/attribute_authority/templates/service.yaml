apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_attribute_authority.fullname" . }}
  labels:
    {{- include "service_attribute_authority.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_attribute_authority.selectorLabels" . | nindent 4 }}
