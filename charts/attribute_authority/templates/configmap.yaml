apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "service_attribute_authority.name" . }}-cm
data:
  POSTGRES_HOST: {{ .Values.postgres.host }}
  POSTGRES_USER: {{ .Values.postgres.user }}
  {{/*  POSTGRES_PASSWORD: deployment.envFrom.secretRef.attribute-authority-secrets*/}}
  POSTGRES_DATABASE: {{ .Values.postgres.database }}
  POSTGRES_SCHEMA: {{ .Values.postgres.schema }}
