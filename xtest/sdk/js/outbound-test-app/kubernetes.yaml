apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: opentdf
    app.kubernetes.io/name: outbound-app
  name: opentdf-outbound-demo-react-app
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
  selector:
    app.kubernetes.io/instance: opentdf
    app.kubernetes.io/name: outbound-app
  type: ClusterIP
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: opentdf-outbound-demo-react-app
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$2
  name: opentdf-outbound-demo-react-app
spec:
  rules:
    - host: localhost
      http:
        paths:
          - backend:
              service:
                name: opentdf-outbound-demo-react-app
                port:
                  number: 80
            path: /outbound-app(/|$)(.*)
            pathType: Prefix
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: opentdf-outbound-demo-react-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: opentdf
      app.kubernetes.io/name: outbound-app
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: opentdf
        app.kubernetes.io/name: outbound-app
    spec:
      serviceAccountName: opentdf-outbound-demo-react-app
      containers:
        - name: outbound-app
          image: opentdf/outbound-app
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /outbound-app(/|$)(.*)
              port: http
          readinessProbe:
            httpGet:
              path: /outbound-app(/|$)(.*)
              port: http
