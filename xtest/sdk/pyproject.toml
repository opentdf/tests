[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "xtest-sdk"
version = "0.1.0"
description = "OpenTDF SDK configuration and testing utilities"
requires-python = ">=3.11"
dependencies = [
    "GitPython==3.1.46",
]
