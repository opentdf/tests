apiVersion: opentdf.io/v1alpha1
kind: Feature
metadata:
  name: mechanism-mlkem
  jira: DSPX-2399
  title: "Pure ML-KEM-768 post-quantum key encapsulation mechanism"
  created: 2026-05-18
repos:
  tests:
    branch: DSPX-2399-tests
    todo:
      - Add "mechanism-mlkem" to feature_type Literal in xtest/tdfs.py (alphabetically after mechanism-ec-curves-384-521)
      - Add platform version gate (self.semver >= (0, 15, 0)) in PlatformFeatureSet.__init__ — version TBD
      - Add "mlkem:768" to kas_algorithm_type in xtest/abac.py + KAS_PUBLIC_KEY_ALG_ENUM_MLKEM_768 = 13 + mapping entries
      - Add key_mlkem768 fixture in xtest/fixtures/keys.py
      - Add attribute_with_mlkem768_key fixture in xtest/fixtures/keys.py
      - Add test_mlkem768_roundtrip in xtest/test_pqc.py
      - Wire format — KAO type "wrapped"; wrappedKey = base64(ml_kem_ciphertext [1088 bytes] || aes_wrapped_dek); no ephemeralPublicKey
  platform-proto:
    path: platform
    branch: DSPX-2399-platform-proto
    todo:
      - Add KAS_PUBLIC_KEY_ALG_ENUM_MLKEM_768 = 13 to KasPublicKeyAlgEnum in service/policy/objects.proto
      - Run buf generate; commit regenerated Go/Java/JS stubs
  platform-service:
    path: platform
    branch: DSPX-2399-platform-service
    depends_on: [platform-proto]
    todo:
      - Add mlkem:768 KeyType to lib/ocrypto (or integrate a Go ML-KEM library)
      - Implement ML-KEM-768 decapsulation in service/kas/access/rewrap.go; split wrappedKey at byte 1088 to extract ciphertext; decapsulate to recover shared secret; unwrap DEK from remainder
      - Add convertAlgEnum2Simple / convertStringToAlgorithm mappings for KAS_PUBLIC_KEY_ALG_ENUM_MLKEM_768
      - Gate behind preview flag in opentdf.yaml / opentdf-dev.yaml if needed; ensure test harness enables it
  platform-go-sdk:
    path: platform
    branch: DSPX-2399-platform-go-sdk
    depends_on: [platform-proto, platform-service]
    todo:
      - Implement ML-KEM-768 client-side encapsulation; emit KAO type "wrapped" with wrappedKey = base64(ml_kem_ciphertext [1088 bytes] || aes_wrapped_dek); no ephemeralPublicKey field
      - Add `supports mechanism-mlkem` case to xtest/sdk/go/cli.sh (grep for mlkem:768 in `help policy kas-registry key create`)
  java-sdk:
    path: java-sdk
    branch: DSPX-2399-java-sdk
    depends_on: [platform-proto]
    todo:
      - Add MLKEM_768 entry to KeyType.java enum + fromPublicKeyAlgorithm / fromAlgorithm mappings
      - Implement ML-KEM-768 encapsulation (Bouncy Castle post-quantum provider); same wire format as Go SDK
      - Add `supports mechanism-mlkem` case to xtest/sdk/java/cli.sh
  web-sdk:
    path: web-sdk
    branch: DSPX-2399-web-sdk
    depends_on: [platform-proto]
    todo:
      - Add "mlkem:768" to KasPublicKeyAlgorithm union in lib/src/access.ts
      - Add ML-KEM-768 enum entry in lib/src/crypto/enums.ts
      - Implement encapsulation/decapsulation via @noble/post-quantum or WASM library; same wire format
      - Add `supports mechanism-mlkem` case to xtest/sdk/js/cli.sh
scenarios:
  - xtest/scenarios/mechanism-mlkem.yaml
